
InnoDB --> Sistema Transaccional

Consistencia= Por el camino no puede pasar nada
Atomicidad= no puede pasar por más de dos personas
Aislamient= 2 transaciónes no se hacen a la vez
Definidad= una transacion si es confirmada se guardada para poder ver por donde a pasado
ISAM

sql
 create database <nombreBD>;
 use <nombreBD>;
 create table <nomtrable>( metemos dentro los atributos);
 Types
 	numericos
	 	interger/int
 		smallint
 		decimal
 		numeric
 	fechas
 		date
 		time
 		datetime
 		timestamp
 		year
 	caracteres
 		char
 		varchar
 		enum
 		text
